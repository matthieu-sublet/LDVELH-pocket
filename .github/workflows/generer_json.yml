name: Generer Inventaire JSON
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Generer JSON
        run: python generer_json.py

      - name: Commit et Push
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          
          # Vérification avec le bon nom de fichier : books-list.json
          if [ -f "books-list.json" ]; then
            git add books-list.json
            git commit -m "Mise à jour automatique de la liste des livres" || echo "Aucun changement à commiter"
            git push
          else
            echo "Erreur : Le fichier books-list.json n'a pas été généré par le script Python."
            exit 1
          fi
